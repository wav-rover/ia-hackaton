# Generated by Stackr. Adjust values as needed.

# Postgres connection string (matches the bundled docker-compose.yml)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app?schema=public

# Secret used by Auth.js to encrypt JWTs. Keep this private.
AUTH_SECRET=
# --- Serveur du modèle IA (Ollama, VM 306) ---
# URL de base de l'API Ollama. Laisser vide => réponse simulée.
# Exemple direct sur la VM : http://10.0.0.42:11434
OLLAMA_BASE_URL=
# Obligatoire si OLLAMA_BASE_URL est renseigné (cf. `ollama list` sur la VM).
OLLAMA_MODEL=phi35-financial:latest
